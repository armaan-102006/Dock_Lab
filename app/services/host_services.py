
from models.user import User

def node_url_update(url, user):
    #change the tokens to be issued to both the host and client users differently(meaning issue aud, not for this function)
    #update the url to respective user
    pass